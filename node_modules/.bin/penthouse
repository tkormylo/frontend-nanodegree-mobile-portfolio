#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../penthouse/bin/penthouse" "$@"
  ret=$?
else 
  node  "$basedir/../penthouse/bin/penthouse" "$@"
  ret=$?
fi
exit $ret
